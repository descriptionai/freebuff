// ============================================================
// 노선별 네비게이션 — 설정 (카카오 로컬 API 기반)
// ============================================================
// ★ API 키는 js/config.keys.js 에서 로드됩니다.
//   config.keys.js 가 없으면 빈 키로 동작하며 카카오 검색이 불가합니다.
//
// ★ 동작 방식:
//   1. 페이지 로드 시 카카오 로컬 API로 "대전 우체국", "대전 우편취급국" 검색
//   2. 검색 결과에서 좌표·주소·전화번호를 자동 수집
//   3. 수집한 좌표로 Clarke-Wright + 2-opt 최적 경로 계산
//   4. 카카오 지도에 경로 표시 (OSRM 실제 도로 경로)
// ============================================================

window.NODE_NAV_CONFIG = {

  /* ---------- 지도 프로바이더 ---------- */
  mapProvider: 'kakao',

  /* ---------- 카카오 API ---------- */
  kakao: {
    appkey:    (window.KAKAO_KEYS && window.KAKAO_KEYS.appkey)    || '',
    restApiKey: (window.KAKAO_KEYS && window.KAKAO_KEYS.restApiKey) || ''
  },

  /* ---------- 출발/복귀 지점: 중부권광역우편물류센터 ---------- */
  depot: {
    name: '중부권광역우편물류센터',
    addr: '대전 동구 안골로 11',
    lat: 36.2706944,
    lng: 127.4733805
  },

  /* ---------- 카카오 로컬 API 검색 설정 ---------- */
  search: {
    // 검색 키워드 (순서대로 검색 → 결과 합침)
    keywords: [
      '대전 우체국',
      '대전 우편취급국'
    ],
    // 대전시 경계 (서,남,동,북) — 이 범위 내 결과만 수집
    rect: '127.25,36.23,127.55,36.48',
    pageSize: 15  // 카카오 API 최대 15
  },

  /* ---------- 노선 최적화 기준 ---------- */
  maxStopsPerRoute: 8,
  maxKmPerRoute: 60,
  roadRouting: true,
  osrmUrl: 'https://router.project-osrm.org',
  roadFactor: 1.3
};
